# Generated by scripts/vendor-stdlib.sh. Do not edit.
#
# These files are copies. Change them in lumen-effects and re-vendor; editing
# them here makes these checksums lie.
#
# NOTE: v1 has not yet been vendored from a real lumen-effects tag - that repo's
# stdlib is still being written. This entry records the interim copy so the
# checksum discipline exists from the start, and the tag below says so plainly
# rather than naming a tag that does not exist.

source = "lumen-effects"
tag = "UNRELEASED-interim-copy"
version = "v1"

[[file]]
name = "core.lfx"
sha256 = "164746d96d795d6b185deae6df969579ee199028b6ed066324513a7e3111fe62"
