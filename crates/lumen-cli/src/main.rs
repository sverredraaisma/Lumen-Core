//! `lumen` — compile, budget, publish, backup. Skeleton (W10).
//!
//! Stays Apache-licensed only for as long as it restricts itself to compiling
//! and publishing *over the protocol*. The moment it wants to join the mesh as
//! a participant it links `lumen-device`, and belongs on the GPL side.

fn main() {
    println!(
        "lumen {} — no subcommands implemented yet",
        env!("CARGO_PKG_VERSION")
    );
}
