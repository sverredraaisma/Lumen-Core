#!/usr/bin/env bash
# Vendor a stdlib version from a lumen-effects tag into stdlib/<version>/ and
# rewrite the checksum manifest. Skeleton — implemented with W15.
#
# Usage: scripts/vendor-stdlib.sh <tag> [version-dir]
set -euo pipefail
echo "not implemented yet: would vendor lumen-effects tag ${1:-<none>}" >&2
exit 1
